# 商业化交付验收清单

更新时间：2026-04-25

## 封板标准

- 后端全量测试通过：`PYTHONPATH=. pytest -q`
- 前端脚本语法通过：`node --check front/ui_utils.js front/api.js front/dataset_upload.js front/imageset_preview.js front/gallery.js front/class_mapping.js front/annotate.js front/qwen_annotate.js front/jobs_history.js front/refine.js front/train.js front/preview.js front/app.js`
- Diff 基础检查通过：`git diff --check`
- 系统自检通过：登录后访问 `/api/system/readiness`，`operational=true`
- 生产交付前：`/api/system/readiness` 中 `production_ready=true`

## 必验流程

1. 上传标准 YOLO 数据集，包含 `images/`、`labels/`、`data.yaml` 或 `labels/classes.txt`。
2. 确认导入结果显示图片数、标签数、类别数，类别中文名称能在预览中展示。
3. 使用权重自动打标，完成后打开“最近结果对照画廊”，检查原图、Overlay、标签 TXT。
4. 使用 AI 打标跑小样本，确认失败提示可读，成功结果可回写到图片集。
5. 进入精修模块，修改一个框并保存，刷新后确认标签仍存在。
6. 训练模块选择数据集启动小轮数训练，确认 best.pt 可下载，训练后预测预览可展示。
7. 在所有数据集/权重选择处验证“改名”和“下载”按钮。
8. 删除/清理历史任务产物后，确认不会再显示失效的“最近结果对照画廊”链接。

## 生产配置

生产环境必须设置：

```bash
AUTOANNOTATION_ADMIN_USERNAME=客户管理员账号
AUTOANNOTATION_ADMIN_PASSWORD=强密码
AUTOANNOTATION_SESSION_SECRET=随机长字符串
AUTOANNOTATION_LICENSE_UNLOCK_KEY=客户独立解锁密钥
AUTOANNOTATION_CORS_ORIGINS=http://客户域名或IP:端口
```

如需 AI 打标，还必须设置：

```bash
AUTOANNOTATION_QWEN_API_KEY=客户或交付方 API Key
```

## 源码交付边界

- 源码仓库只交付代码、配置模板、测试、部署说明。
- Docker 镜像包、zip、tar、模型权重不进入源码版本控制。
- 本地已有大包可保留，但正式源码包应通过 `.gitignore` 和发布流程排除。
