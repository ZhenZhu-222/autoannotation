# 产品商业化评估与改进清单

更新时间：2026-04-25

## 结论

当前产品已经具备“内测/交付试点”的核心能力：视频抽帧、图片集管理、模型打标、AI 打标、人工精修、类 ID 调整、训练与导出。但还不建议直接作为商业化正式版交付给陌生客户。

主要原因不是单点功能缺失，而是工程稳定性、数据兼容性、前端可维护性、部署包卫生和错误恢复能力还不够产品化。

一句话判断：

- **能不能商业化：能，但应先做 1 轮工程清理和稳定性封板。**
- **现在合不合格：作为 Demo/内测合格；作为正式商业交付不合格。**
- **代码脏不脏：有明显脏点，尤其是前端单文件过大、服务层职责混杂、仓库混入大包产物。**

## 必须改（商业化前 P0）

1. **数据集导入兼容性**
   - 文件夹上传必须完整识别 `images/`、`labels/`、`classes.txt`、`data.yaml`。
   - 同名图片/标签必须按相对目录匹配，不能只按 basename。
   - 上传结果要明确提示：导入图片数、标签数、类别名数、未匹配标签样例。

2. **导入/导出一致性**
   - 平台内图片集目录统一输出 `images/`、`labels/`、`labels/classes.txt`、`data.yaml`。
   - 类别名解析优先级统一：当前数据集文件优先，再看历史任务/模型来源。
   - 训练、预览、精修、下载必须读同一套类别名来源，不能各自推断。

3. **错误提示产品化**
   - 上传失败不能只显示接口错误，要显示可操作原因：文件过大、无图片、标签未匹配、类别文件为空/格式错误。
   - AI 打标、训练、导出失败要保留 job 错误详情和可复制诊断信息。

4. **部署包清理**
   - 仓库不应长期跟踪 Docker tar、zip、模型权重等大体积产物。
   - 发布包应从构建流程生成，不应和源码混在同一提交里。

5. **基础安全**
   - 默认账号密码、License key、会话密钥不能硬编码为真实交付默认值。
   - 生产环境必须要求用户首次启动修改管理员密码。
   - 上传文件路径必须继续保持目录穿越防护。

## 应该改（正式版 P1）

1. **前端拆分**
   - `front/app.js` 已超过 3500 行，建议拆成：API 层、图片集模块、打标模块、AI 打标模块、精修编辑器、训练模块、公共 UI 工具。
   - 拆分后再引入最小构建工具或继续保持无构建，但不能让单文件继续膨胀。

2. **服务层职责收敛**
   - `annotation_service.py`、`qwen_service.py`、`qwen_annotation_service.py` 已过大，建议按“解析 / 执行 / 写结果 / 导出 / 回滚”拆开。
   - 图片集导入逻辑建议独立成 `dataset_import_service.py`，避免 `imageset_service.py` 继续变重。

3. **任务与历史可观测性**
   - 每个 job 增加统一 `run_meta.json`、错误码、输入摘要、输出摘要。
   - 前端历史列表支持按图片集、任务类型、状态筛选。

4. **测试分层**
   - 保留端到端测试，同时补轻量单元测试：类别名解析、YOLO 标签解析、目录匹配、导出结构。
   - 对“上传真实 YOLO 数据集 → 精修 → 训练 → 下载”建立固定冒烟用例。

5. **产品文案统一**
   - “千问打标”建议统一成“AI 打标”，底层供应商名称放在配置里。
   - 操作按钮、状态文案、错误文案要统一中文表达。

## 可后续改（P2）

1. **多用户/团队能力**
   - 当前更像单机工具；商业化 SaaS/团队版需要用户、项目、权限、审计日志。

2. **数据资产管理**
   - 增加数据集版本、标签版本、训练版本、模型版本之间的可追踪关系。

3. **质量验收面板**
   - 增加标签覆盖率、类别分布、空标签图片、异常 class id、图片尺寸分布等统计。

4. **模型评估闭环**
   - 训练后自动生成验证预览、误检/漏检样例、可下载评估报告。

## 本轮已处理的问题

本轮重点修复“上传文件夹数据集只读图片和标签，不读标签名”的问题：

- 支持导入 `classes.txt` / `names.txt` / `obj.names`。
- 支持导入 `data.yaml` / `data.yml` 的 `names` 和 `task`。
- 前端上传保留目录相对路径，后端按相对路径匹配图片和标签。
- 上传响应增加类别名导入数量和任务类型。
- 补充回归测试覆盖类别名导入、`data.yaml` 导入、重复 basename 匹配。

本轮商业化封板第一批也已推进：

- 将数据集导入解析逻辑抽离为独立服务，降低 `imageset_service.py` 继续膨胀的风险。
- 拆分前端 API、公共 UI 工具、文件夹上传、图片集预览脚本，保留无构建直连加载方式。
- 增加发布产物 `.gitignore` 规则，避免 Docker tar/zip/模型权重继续污染源码 diff。
- 增加生产安全提示，`/api/system/session` 会返回默认密码、默认解锁密钥、未配置会话密钥、开放 CORS 等风险。
- 更新 README，明确 YOLO 数据集导入格式、生产环境变量、发布产物与源码边界。
- 统一推理工厂与标签任务规格入口，减少后续新增模型类型/标签任务时的分散判断。
- 补齐 `/api/system/readiness` 交付自检、商业化验收清单和一键验收脚本，避免只靠人工口头判断是否成品。

## 建议下一轮封板目标

建议下一轮不要继续堆新功能，先做“商业化封板”：

1. 继续拆分前端大文件，把模型管理、任务历史、精修编辑器分模块。
2. 清理历史已跟踪的大包产物，建立正式发布流水线或发布附件流程。
3. 拆分打标与 AI 打标服务，降低单个 service 过大的维护风险。
4. 建立一组真实样例冒烟测试，覆盖上传、打标、精修、训练、下载。
5. 统一产品文案和错误码，形成客户可读的排障说明。
